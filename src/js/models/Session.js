import HtmlModel from "./HtmlModel";

class Session extends HtmlModel {
    
    constructor(rawHtml) {
        super(rawHtml);
    }

    validate(rawHtml) {
        // ...
    }
}

export default Session;